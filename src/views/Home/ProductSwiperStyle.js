import * as React from "react";
import { styled } from "@mui/system";

export const SwiperProduct = styled("div")({
  width: "85rem",
  margin: "auto",
  height: "5rem",
  padding: "20px",
  borderRadius: "10px",
});
